events {
    worker_connections 1024;
}

http {
    server {
        listen 80;
        location / {
            return 200 'BancoZim Banking System - Service Temporarily Unavailable';
            add_header Content-Type text/plain;
        }
        location /health {
            return 200 'healthy';
            add_header Content-Type text/plain;
        }
    }
}